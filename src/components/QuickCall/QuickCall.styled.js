import styled from "styled-components";

const StyledQuickCall = styled.a`
   margin-left: auto;
   display: flex;
   align-items: center;
   gap: 0.5em;
   font-size: 1.8rem;

   svg {
      width: 2.4rem;
      height: 2.4rem;
      transform: translateY(-1.5px);
   }
`;

export default StyledQuickCall;
